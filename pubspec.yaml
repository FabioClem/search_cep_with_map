name: flutter_application_clean
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.4.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  bloc: ^8.1.4
  flutter_bloc: ^8.1.6
  http: ^1.2.1
  get_it: ^7.7.0
  url_launcher: ^6.3.0
  url_launcher_web: ^2.3.1
  flutter_map: ^7.0.1
  latlong2: ^0.9.1
  flutter_map_cancellable_tile_provider: ^3.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
